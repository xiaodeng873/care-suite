# 出入量記錄模態框重新設計

## 📋 需求概述

重新設計出入量(Intake/Output)記錄模態框，支持動態增減項目，Web和Mobile互聯互通。

---

## 🎨 UI 布局設計

### Mobile 布局 (垂直滾動)

```
┌─────────────────────────────────────────┐
│  ✕  出入量記錄 - 2025/12/22 08:00      │  ← 標題欄
├─────────────────────────────────────────┤
│                                         │
│  ┌─ 攝入 (Intake) ──────────────────┐  │
│  │                                   │  │
│  │  餐膳 Meals                       │  │
│  │  ┌─────────────────────────────┐ │  │
│  │  │ + 新增餐膳項目                │ │  │  ← 可展開/收起
│  │  └─────────────────────────────┘ │  │
│  │  ┌───────────────────────────────┐│  │
│  │  │ 🍚 早餐         1/2    [刪除] ││  │  ← 已添加項目
│  │  │ 🍚 午餐         3/4    [刪除] ││  │
│  │  └───────────────────────────────┘│  │
│  │                                   │  │
│  │  飲料 Beverages                   │  │
│  │  ┌─────────────────────────────┐ │  │
│  │  │ + 新增飲料項目                │ │  │
│  │  └─────────────────────────────┘ │  │
│  │  ┌───────────────────────────────┐│  │
│  │  │ 💧 水          200ml  [刪除]  ││  │
│  │  │ 🥛 奶          150ml  [刪除]  ││  │
│  │  └───────────────────────────────┘│  │
│  │                                   │  │
│  │  其他 Others                      │  │
│  │  ┌─────────────────────────────┐ │  │
│  │  │ + 新增其他項目                │ │  │
│  │  └─────────────────────────────┘ │  │
│  │  ┌───────────────────────────────┐│  │
│  │  │ 🍪 餅乾        3塊    [刪除]  ││  │
│  │  └───────────────────────────────┘│  │
│  │                                   │  │
│  │  鼻胃飼 Tube Feeding              │  │
│  │  ┌─────────────────────────────┐ │  │
│  │  │ + 新增鼻胃飼項目              │ │  │
│  │  └─────────────────────────────┘ │  │
│  │  ┌───────────────────────────────┐│  │
│  │  │ 💊 Isocal     250ml  [刪除]  ││  │
│  │  └───────────────────────────────┘│  │
│  │                                   │  │
│  │  小計: 1.25份餐 + 350ml + 3塊    │  │  ← 分類小計
│  └───────────────────────────────────┘  │
│                                         │
│  ┌─ 排出 (Output) ────────────────┐   │
│  │                                   │  │
│  │  尿液 Urine                       │  │
│  │  ┌─────────────────────────────┐ │  │
│  │  │ + 新增尿液記錄                │ │  │
│  │  └─────────────────────────────┘ │  │
│  │  ┌───────────────────────────────┐│  │
│  │  │ 💧 黃色        300ml  [刪除]  ││  │
│  │  │ 💧 透明        150ml  [刪除]  ││  │
│  │  └───────────────────────────────┘│  │
│  │                                   │  │
│  │  胃液 Gastric                     │  │
│  │  ┌─────────────────────────────┐ │  │
│  │  │ + 新增胃液記錄                │ │  │
│  │  └─────────────────────────────┘ │  │
│  │  ┌───────────────────────────────┐│  │
│  │  │ 🧪 啡色 pH:4  100ml  [刪除]  ││  │
│  │  └───────────────────────────────┘│  │
│  │                                   │  │
│  │  小計: 550ml                      │  │  ← 分類小計
│  └───────────────────────────────────┘  │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ 記錄人: 張護士                   │   │
│  │ 備註: [輸入備註...]              │   │
│  └─────────────────────────────────┘   │
│                                         │
│  [ 取消 ]           [ 儲存記錄 ]       │  ← 操作按鈕
└─────────────────────────────────────────┘
```

### 新增項目面板設計 (Picker/Modal)

```
┌─────────────────────────────────────────┐
│  新增 - 餐膳                             │
├─────────────────────────────────────────┤
│                                         │
│  類型                                   │
│  ┌─────────────────────────────────┐   │
│  │ 早餐                     ▼      │   │  ← Picker
│  └─────────────────────────────────┘   │
│  選項: 早餐/午餐/下午茶/晚餐            │
│                                         │
│  份量                                   │
│  ┌─────────────────────────────────┐   │
│  │ 1/2                      ▼      │   │  ← Picker
│  └─────────────────────────────────┘   │
│  選項: 1, 3/4, 1/2, 1/4                │
│                                         │
│  [ 取消 ]              [ 新增 ]        │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  新增 - 飲料                             │
├─────────────────────────────────────────┤
│  類型                                   │
│  ┌─────────────────────────────────┐   │
│  │ 水                       ▼      │   │
│  └─────────────────────────────────┘   │
│  選項: 水/湯/奶/果汁/糖水/茶            │
│                                         │
│  容量 (ml)                              │
│  ┌─────────────────────────────────┐   │
│  │ 200                             │   │  ← 數字輸入
│  └─────────────────────────────────┘   │
│                                         │
│  [ 取消 ]              [ 新增 ]        │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  新增 - 尿液                             │
├─────────────────────────────────────────┤
│  顏色                                   │
│  ┌─────────────────────────────────┐   │
│  │ 黃                       ▼      │   │
│  └─────────────────────────────────┘   │
│  選項: 透明/黃/啡/紅                    │
│                                         │
│  容量 (ml)                              │
│  ┌─────────────────────────────────┐   │
│  │ 300                             │   │
│  └─────────────────────────────────┘   │
│                                         │
│  [ 取消 ]              [ 新增 ]        │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  新增 - 胃液                             │
├─────────────────────────────────────────┤
│  顏色                                   │
│  ┌─────────────────────────────────┐   │
│  │ 啡                       ▼      │   │
│  └─────────────────────────────────┘   │
│                                         │
│  pH值                                   │
│  ┌─────────────────────────────────┐   │
│  │ 4.5                             │   │  ← 數字輸入
│  └─────────────────────────────────┘   │
│                                         │
│  容量 (ml)                              │
│  ┌─────────────────────────────────┐   │
│  │ 100                             │   │
│  └─────────────────────────────────┘   │
│                                         │
│  [ 取消 ]              [ 新增 ]        │
└─────────────────────────────────────────┘
```

---

## 🗄️ 數據結構設計

### Database Schema

```sql
-- 出入量主記錄表
CREATE TABLE intake_output_records (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  patient_id INTEGER NOT NULL REFERENCES patients(院友id),
  record_date DATE NOT NULL,
  time_slot VARCHAR(10) NOT NULL, -- '07:00', '08:00', '09:00', ... '06:00' (一天从07:00开始)
  recorder VARCHAR(100) NOT NULL,
  notes TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 攝入項目表 (動態)
CREATE TABLE intake_items (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  record_id UUID NOT NULL REFERENCES intake_output_records(id) ON DELETE CASCADE,
  category VARCHAR(20) NOT NULL, -- 'meal', 'beverage', 'other', 'tube_feeding'
  item_type VARCHAR(50) NOT NULL, -- 早餐/午餐/水/湯/餅乾/Isocal 等
  amount VARCHAR(20) NOT NULL, -- '1/2', '200ml', '3塊' 等
  amount_numeric DECIMAL(10,2), -- 用於計算的數值
  unit VARCHAR(10) NOT NULL, -- 'portion', 'ml', 'piece' 等
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 排出項目表 (動態)
CREATE TABLE output_items (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  record_id UUID NOT NULL REFERENCES intake_output_records(id) ON DELETE CASCADE,
  category VARCHAR(20) NOT NULL, -- 'urine', 'gastric'
  color VARCHAR(20), -- 透明/黃/啡/紅
  ph_value DECIMAL(3,1), -- pH值 (僅胃液)
  amount_ml INTEGER NOT NULL, -- 容量(ml)
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 索引
CREATE INDEX idx_io_records_patient_date ON intake_output_records(patient_id, record_date);
CREATE INDEX idx_intake_items_record ON intake_items(record_id);
CREATE INDEX idx_output_items_record ON output_items(record_id);
```

### TypeScript Interfaces

```typescript
// 主記錄
interface IntakeOutputRecord {
  id: string;
  patient_id: number;
  record_date: string;
  time_slot: string;
  recorder: string;
  notes?: string;
  created_at: string;
  updated_at: string;
  // 關聯數據
  intake_items?: IntakeItem[];
  output_items?: OutputItem[];
}

// 攝入項目
interface IntakeItem {
  id: string;
  record_id: string;
  category: 'meal' | 'beverage' | 'other' | 'tube_feeding';
  item_type: string;
  amount: string; // 顯示用
  amount_numeric: number; // 計算用
  unit: 'portion' | 'ml' | 'piece';
  created_at: string;
}

// 排出項目
interface OutputItem {
  id: string;
  record_id: string;
  category: 'urine' | 'gastric';
  color?: string;
  ph_value?: number;
  amount_ml: number;
  created_at: string;
}

// 選項配置
const INTAKE_OPTIONS = {
  meal: {
    label: '餐膳',
    types: ['早餐', '午餐', '下午茶', '晚餐'],
    amounts: ['1', '3/4', '1/2', '1/4'],
    unit: 'portion',
    icon: '🍚'
  },
  beverage: {
    label: '飲料',
    types: ['水', '湯', '奶', '果汁', '糖水', '茶'],
    unit: 'ml',
    icon: '💧'
  },
  other: {
    label: '其他',
    types: ['餅乾', '點心', '零食', '甜品'],
    units: ['塊', '粒'],
    icon: '🍪'
  },
  tube_feeding: {
    label: '鼻胃飼',
    types: ['Isocal', 'Ultracal', 'Glucerna', 'Isosource', 'Compleat'],
    unit: 'ml',
    icon: '💊'
  }
};

const OUTPUT_OPTIONS = {
  urine: {
    label: '尿液',
    colors: ['透明', '黃', '啡', '紅'],
    icon: '💧'
  },
  gastric: {
    label: '胃液',
    colors: ['透明', '黃', '啡', '紅'],
    hasPH: true,
    icon: '🧪'
  }
};
```

---

## 📱 組件架構

### Mobile Components

```
IntakeOutputModal/
├── IntakeOutputModal.tsx          (主模態框)
├── IntakeSection.tsx              (攝入區塊)
│   ├── MealCategory.tsx           (餐膳分類)
│   ├── BeverageCategory.tsx       (飲料分類)
│   ├── OtherCategory.tsx          (其他分類)
│   └── TubeFeedingCategory.tsx    (鼻胃飼分類)
├── OutputSection.tsx              (排出區塊)
│   ├── UrineCategory.tsx          (尿液分類)
│   └── GastricCategory.tsx        (胃液分類)
├── AddItemModal.tsx               (新增項目子模態框)
└── IntakeOutputItem.tsx           (項目卡片組件)

Styles:
└── intakeOutputStyles.ts
```

### Web Components

```
IntakeOutputDialog/
├── IntakeOutputDialog.tsx         (主對話框)
├── IntakePanel.tsx                (攝入面板)
├── OutputPanel.tsx                (排出面板)
├── CategorySection.tsx            (分類區塊 - 可復用)
├── AddItemDialog.tsx              (新增項目對話框)
└── ItemCard.tsx                   (項目卡片)

Styles:
└── intakeOutputDialog.module.css
```

---

## 🔄 交互流程

### 1. 新增記錄流程
```
用戶點擊時段 → 開啟模態框 → 
  選擇分類(餐膳/飲料等) → 點擊「+ 新增」→ 
  彈出添加面板 → 選擇類型/輸入數值 → 
  確認新增 → 顯示在列表中 → 
  可繼續添加或點擊儲存
```

### 2. 編輯記錄流程
```
點擊已有記錄 → 開啟模態框(載入已有項目) →
  顯示所有已添加項目 → 
  可新增/刪除項目 → 
  修改備註 → 儲存更新
```

### 3. 刪除項目流程
```
點擊項目的[刪除]按鈕 → 
  確認對話框 → 
  從列表移除(未儲存前僅從狀態移除)
```

---

## 💾 數據持久化

### API 設計

```typescript
// 儲存/更新記錄
POST/PUT /api/intake-output/records
{
  patient_id: number,
  record_date: string,
  time_slot: string,
  recorder: string,
  notes?: string,
  intake_items: [
    { category, item_type, amount, amount_numeric, unit }
  ],
  output_items: [
    { category, color, ph_value, amount_ml }
  ]
}

// 獲取記錄
GET /api/intake-output/records?patient_id=X&date=YYYY-MM-DD

// 刪除記錄
DELETE /api/intake-output/records/:id
```

---

## 📊 顯示邏輯

### 主表格顯示
```
在 CareRecordsScreen 的出入量選項卡中:
- 每個時段顯示攝入/排出總量
- 點擊進入詳情模態框
- 顯示匯總: 
  - 攝入: X份餐 + Xml飲料 + X個點心 + Xml鼻胃飼
  - 排出: Xml
```

### 統計計算
```typescript
// 攝入統計
const calculateIntake = (items: IntakeItem[]) => {
  const meals = items.filter(i => i.category === 'meal')
    .reduce((sum, i) => sum + i.amount_numeric, 0);
  const beverages = items.filter(i => i.category === 'beverage')
    .reduce((sum, i) => sum + i.amount_numeric, 0);
  const tubeFeeding = items.filter(i => i.category === 'tube_feeding')
    .reduce((sum, i) => sum + i.amount_numeric, 0);
  const others = items.filter(i => i.category === 'other');
  
  return {
    meals: `${meals}份`,
    liquids: `${beverages + tubeFeeding}ml`,
    others: `${others.length}項`
  };
};

// 排出統計
const calculateOutput = (items: OutputItem[]) => {
  const total = items.reduce((sum, i) => sum + i.amount_ml, 0);
  return `${total}ml`;
};
```

---

## 🎯 實施優先級

### Phase 1: 數據庫遷移
1. ✅ 創建新表結構
2. ✅ 數據遷移腳本

### Phase 2: Mobile 實現
1. 創建新組件架構
2. 實現攝入區塊
3. 實現排出區塊
4. 整合到 CareRecordsScreen

### Phase 3: Web 實現
1. 創建對應組件
2. 實現相同邏輯
3. 確保數據同步

### Phase 4: 測試與優化
1. 功能測試
2. UI/UX 優化
3. 性能優化

---

## 📝 注意事項

1. **數據驗證**: 所有數字輸入需要驗證範圍
2. **單位轉換**: 份量(1/4, 1/2等)需要轉換為數值儲存
3. **多語言**: 所有文字需要支持中英文
4. **離線支持**: 考慮離線緩存未提交的記錄
5. **權限控制**: 僅允許修改自己創建的記錄(或管理員)

---

## 🔧 技術棧

- **Mobile**: React Native, React Native Paper
- **Web**: React, Material-UI / Ant Design
- **Database**: PostgreSQL (Supabase)
- **State Management**: React Hooks (useState, useReducer)
- **Form Validation**: Yup / Zod

