{
  "name": "care-suite",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm -ws run build",
    "dev": "npm -ws run dev",
    "lint": "npm -ws run lint"
  },
  "devDependencies": {
    "@types/react": "~19.1.10",
    "@types/react-dom": "~19.1.10"
  },
  "overrides": {
    "@types/react": "~19.1.10",
    "@types/react-dom": "~19.1.10"
  }
}
