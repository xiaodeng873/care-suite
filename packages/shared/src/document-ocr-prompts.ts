/**
 * 覆診預約／診斷記錄／疫苗記錄 OCR 識別統一 prompt 內核。
 *
 * 與 PRESCRIPTION_OCR_PROMPT_CORE 同一套管理方式：
 * AI 助護 Edge Function 同埋各管理頁面嘅智能識別（OCRDocumentBlock）
 * 都必須用呢個文檔，保證兩個端口運作原理一致。
 *
 * Edge Function 使用鏡像副本：supabase/functions/ai-assistant/_shared/document-ocr-prompts.ts
 * 修改時請先改呢個檔案，再複製覆蓋 _shared 版本。
 */

export const FOLLOWUP_OCR_PROMPT_CORE = `覆診預約便條識別規則：
1. 院友姓名是最重要的識別欄位，必須反覆尋找，不得留空
2. 如有英文姓名或身份證號碼，必須一併提取（用於識別院友）
3. 覆診日期必須為YYYY-MM-DD格式，務必填寫便條上實際日期，找不到才留空
4. 覆診時間、出發時間必須為24小時制HH:MM格式
5. 覆診地點只填醫院或診所名稱，不要填完整地址
6. 備註只填便條上印刷的指示（如「帶備身份證」、「空腹」等）；不要填手寫註記；沒有印刷指示則回傳空字串

需提取欄位（extracted_data 必須使用以下 exact 鍵名，不要自行改寫或合併欄位）：
- 院友姓名
- 英文姓名
- 身份證號碼
- 覆診日期
- 覆診時間
- 覆診地點
- 覆診專科
- 出發時間
- 備註`;

export const DIAGNOSIS_OCR_PROMPT_CORE = `診斷記錄識別規則：
1. 院友姓名是最重要的識別欄位，必須反覆尋找，不得留空
2. 診斷日期必須為YYYY-MM-DD格式，務必填寫文件上實際日期，找不到才留空
3. 診斷項目保持文件原始辨識文字，不要附加中文註解、說明或別名
4. 診斷單位只填醫院、診所或部門名稱，不要填完整地址
5. 如文件包含多筆診斷記錄，頂層欄位填第一筆，並必須同時輸出 records 陣列，每個元素包含：診斷日期、診斷項目、診斷單位；單筆記錄可省略 records

需提取欄位（extracted_data 必須使用以下 exact 鍵名，不要自行改寫或合併欄位）：
- 院友姓名
- 診斷日期
- 診斷項目
- 診斷單位
- records（多筆記錄時使用）`;

export const VACCINATION_OCR_PROMPT_CORE = `疫苗記錄識別規則：
1. 院友姓名是最重要的識別欄位，必須反覆尋找，不得留空
2. 疫苗接種日期必須為YYYY-MM-DD格式，務必填寫文件上實際日期，找不到才留空
3. 疫苗項目保持文件原始辨識文字，不要附加中文註解、說明或別名
4. 接種單位只填醫院、診所或機構名稱，不要填完整地址
5. 如文件包含多筆疫苗記錄，頂層欄位填第一筆，並必須同時輸出 records 陣列，每個元素包含：疫苗接種日期、疫苗項目、接種單位；單筆記錄可省略 records

需提取欄位（extracted_data 必須使用以下 exact 鍵名，不要自行改寫或合併欄位）：
- 院友姓名
- 疫苗接種日期
- 疫苗項目
- 接種單位
- records（多筆記錄時使用）`;
